on: [push]

jobs:
    kicad-generate:
        uses: MissouriMRDT/KiCAD_Reuse_Actions/.github/workflows/kicad.yml@development
        with:
            project-name: "Smart_MoCo"
            board-path: "./Hardware/board"